# Bridge server configuration
# Copy this file to .env and adjust values

# Port for the REST/WebSocket API server
PORT=3000

# Secret token – Home Assistant uses this to authenticate requests.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_TOKEN=change_me_to_a_random_secret

# Where to persist the WhatsApp session so you don't have to re-scan the QR every restart
SESSION_PATH=./.wwebjs_auth
